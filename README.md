# Voxel Thinning

Voxel based thinning and segmentation of a mesh.

## Requirements

This repository uses `Open3d` and `skimages` to convert a given mesh to a voxel grid and then extract sekeleton using `skimage.morphology`.
